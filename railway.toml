# Toss — deploy web and API as two services from this repo (Railway or Coolify).
# See README for Coolify Dockerfile paths, ports, env vars, and Nixpacks commands.
#
# 1. API  - Dockerfile apps/api/Dockerfile,  start: bun run start, port 3001
# 2. Web  - Dockerfile apps/web/Dockerfile,  build: bun run build, start: bun run start
#
# Add Redis and S3-compatible storage.
# Wire REDIS_URL and S3_* env vars on the API service.
# API:  WEB_ORIGIN=https://toss.bramsuurd.nl
# Web:  VITE_API_URL=https://api.toss.bramsuurd.nl  (rebuild after changing)
# After first deploy: cd apps/api && bun run configure-cors
