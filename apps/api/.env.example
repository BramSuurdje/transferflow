PORT=3001
# Exact web app origin (no trailing slash). Production: https://toss.bramsuurd.nl
WEB_ORIGIN=http://localhost:5173

REDIS_URL=redis://localhost:6379

# --- Local MinIO (docker compose) ---
# API in Docker: S3_ENDPOINT=http://minio:9000 and S3_PUBLIC_ENDPOINT=http://localhost:9000
S3_ENDPOINT=http://localhost:9000
# S3_PUBLIC_ENDPOINT=http://localhost:9000
S3_REGION=auto
S3_BUCKET=file-shares
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_FORCE_PATH_STYLE=true

# --- Railway bucket (from Credentials tab) ---
# S3_ENDPOINT=https://t3.storageapi.dev
# S3_REGION=auto
# S3_BUCKET=allocated-pannikin-eotpz3
# S3_ACCESS_KEY_ID=...
# S3_SECRET_ACCESS_KEY=...
# Do NOT set S3_FORCE_PATH_STYLE — virtual-hosted is the default (matches Railway UI).

ENABLE_KEYSPACE_EXPIRY=true
EXPIRY_SWEEPER_INTERVAL_MS=60000

# Optional internal bypass for upload size limits (header x-toss-key or query ?k=).
# INTERNAL_API_KEY=
